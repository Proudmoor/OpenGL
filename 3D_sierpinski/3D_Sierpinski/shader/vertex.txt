#version 150

uniform mat4 projection;
uniform mat4 camera;
uniform mat4 model;

in vec3 vert;
in vec3 vColor;
out vec4 color;

void main() {

gl_Position = projection * camera * model * vec4(vert,1);
color = vec4(vColor, 1.0);

}